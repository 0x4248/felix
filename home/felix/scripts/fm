#!/usr/bin/env bash
set -euo pipefail


ROOT=/root/felix
MENU="$ROOT/felixmenu/dialog_menu.py"

if [ ! -x "$MENU" ]; then
    SCRIPT_DIR="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"
    MENU="$SCRIPT_DIR/../felixmenu/dialog_menu.py"
fi

exec python3 "$MENU" "$@"
